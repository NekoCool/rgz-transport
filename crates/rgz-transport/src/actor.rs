//! Internal actor runtime placeholder.

/// Actor abstraction for future async runtime ownership.
pub struct TransportActor;

impl TransportActor {
    /// Starts the actor loop.
    ///
    /// This is a placeholder for the v2 async actor implementation.
    pub async fn run() {
        // Intentionally empty in the bootstrap phase.
    }
}
